<nav class="navbar navbar-expand-lg bg-body-tertiary px-3 py-2">
  <div class="container-fluid">
    <%= link_to "Stock Tracker", root_path, class: 'navbar-brand' %>
    <% if current_user %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
              aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to 'My Portfolio', my_portfolio_path, class: request.path == my_portfolio_path ? 'nav-link active' : 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'My Friends', my_friends_path, class:  request.path == my_friends_path ? 'nav-link active' : 'nav-link' %>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" aria-disabled="true">Disabled</a>
          </li>
        </ul>
        <ul class="navbar-nav d-flex">
          <li class="nav-item dropdown">
            <a class="btn btn-secondary dropdown-toggle d-flex gap-1 align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <p class="m-0"><%= fa_icon 'user' %></p>
              <p class="m-0"><%= current_user.full_name %></p>
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "#", class: 'dropdown-item d-flex align-items-center gap-1' do %>
                  <p class="m-0"><%= fa_icon 'user' %></p>
                  <p class="m-0">My profile</p>
                <% end %></li>
              <li>
                <%= link_to edit_user_registration_path, class: 'dropdown-item d-flex align-items-center gap-1' do %>
                  <p class="m-0"><%= fa_icon 'edit' %> </p>
                  <p class="m-0">Edit account</p>
                <% end %></li>
              <li>
                <hr class="dropdown-divider">
              </li>
              <li><%= button_to "Sign Out", destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
            </ul>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>